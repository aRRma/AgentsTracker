# Файлы, которые читает Linux внутри контейнера, должны приезжать с LF: с CRLF ломаются
# и оболочка, и разбор переменных окружения.
* text=auto
Dockerfile text eol=lf
*.sh text eol=lf
docker-compose.yml text eol=lf
.env.example text eol=lf
.dockerignore text eol=lf
