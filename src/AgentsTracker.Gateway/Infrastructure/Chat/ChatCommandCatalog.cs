namespace AgentsTracker.Gateway.Infrastructure.Chat;

/// <summary>
/// Список команд для подсказки в интерфейсе канала (у Telegram — кнопка «Меню»): без него бот
/// выглядит как окно без подсказок. Здесь только экраны: /new, /stop, /model, /effort, /mode
/// работают текстом, но в списке их нет — то же самое есть кнопками на экранах «Сессии»
/// и «Агент», а длинный список хуже короткого.
/// </summary>
public static class ChatCommandCatalog
{
    /// <summary>Порядок — по частоте: сначала то, что нужно в каждой сессии, потом настройки, в конце журналы.</summary>
    // Эмодзи — в описании: иконок каналы командам не дают, а имя команды бывает только латиницей.
    // Те же значки, что на кнопках экранов, — чтобы список и меню читались как одно.
    public static readonly IReadOnlyList<ChatCommand> Commands =
    [
        new("menu", "⚙️ меню"),
        new("status", "📟 статус"),
        new("sessions", "🧵 сессия"),
        new("agent", "🤖 агент"),
        new("skills", "🧩 скиллы"),
        new("project", "📁 проект"),
        new("usage", "📊 лимиты и расход"),
        new("rules", "♾ правила «всегда»"),
        new("audit", "📜 журнал"),
        new("help", "❓ справка"),
    ];
}
